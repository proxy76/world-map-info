export default function Menu({ country }) {
    return (
        <div className="backgroundMenu">
            <div className="countryMenu">
                <h1>{country}</h1>
            </div>
        </div>

    );
}